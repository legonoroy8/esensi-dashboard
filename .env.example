# PostgreSQL Database Configuration (Host machine)
DB_HOST=host.docker.internal
DB_PORT=5432
DB_NAME=esensi_db
DB_USER=dashboard_reader
DB_PASSWORD=changeme

# Authentication
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=supersecret

# Session
SESSION_SECRET=change-this-to-random-string-in-production

# Application
PORT=3000
NODE_ENV=production
